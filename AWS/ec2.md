EC2 
elastic cloud compute mainly deals with virtual server in the cloud

mainly cpu,memory which os

key pair as we need to access the virtual instance through cli (ssh access)

then we can setup server with proper os memory and instance type and key pair and then bu hhtp and access from anywhere 

User data - to perforn some tasks when instance runs and then to start the instance

Security groups - to control access to instance (inbound and outbound traffic) (instance pov)

we define rule for port and protocol and source and destination

we can also create our security group for our instance and then attach it to our instance like inbound and out bound rules in EC2 dashboard also we can manage our security group from dashboard 

we can set up ssh access from anywhere to our instance (from key pair what we have downloaded why creating a instance)

delete instance when not in use