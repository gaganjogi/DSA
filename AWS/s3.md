Amazon Simple Storage Service (S3)

simple way to store manage and retrive any amount of data like photo video and document anything

just add unique name while creating bucket like gmail account backups and

store data as objects
global uniquely name

region specific

object with an bucket is stored as key value pair

max object size 5TB

in a region if we have 3 AZ then the data will be replicate in 3 AZ

HOST STATIC WEBSITE

in properties we have option to render static website add html file and then save 
we can see forbidden error this is due to permisssion issue

we enabled permission for public access

then again same issue we need to set bucket policy

so why manage access? this is additional layer of security like security group 

in json format attach directly to service who can access the s3 objects

policy we can directly write using policy generator or custom json policy

get object policy
put object policy

take arn and create policy generator 
give access to inside this bucket 
"arn:aws:s3:::amzn-practise-bucket-489/*"


s3 versioning based on data give versions for every change this is very good


S3 storage classes

standard - most expensive 
s3 intelligent- switch between cost saving


bucket replication

if any current version data is deleted then it will shifts to earlier version 

s3 bucket life cycle
you can use policies to control the movement of data in s3 bucket movement between different storage classes or delete them entirely

and we can also create life cycle of the objects like based on the cost and scenarion we will be moving from standard to intelligent and also we will provide number of days after which it will be deleted

Expire current versions of objects
or transistion between the storage classes


then if we want to upload more data like 10 TB aws offers physical device we can use to storage class

snowcone - few TB
snowball - small petabyte
snowmobile - entire datacenter


aws s3 storage gateway - if we want ot access s3 bucket from on-premises

encryption is required 