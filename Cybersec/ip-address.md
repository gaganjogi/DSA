ipv4 ipv6 
 type of ip address 
private  - LAN (nick name)
public - WAN (real name)

private will not talk with each other
only private - private 
public - public 


static ip address - server we pay for that 
but mobile data :- if we referesh based on availability it will be changes 

media access control address:- 

48 bits 
organisation first pair 
network interface controller - device id second pair
3 pair combination

we need to have ports and packet should be sent and in one port line only one message can be sent only after completing the message cycle

ports can be self defined
well known ports 
1-1023
registered ports 
1024-49151
dynamic ports 
49152-65535

protocols should follow some rules and regulation


tcp - urg flag task need to be done

    psh when to transfer two files
    whatsapp tick ackonowled
    syc

    three way handshake 
    then same way session creation and delete the session
    syn
    syn+ack
    ack

    The PSH (Push) flag in TCP is used to tell the receiving end: "don't buffer this data — deliver it to the application immediately."

    udp directly will send

