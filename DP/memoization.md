when recursion base condition is there converting to top down intialization 

TOP down approach matrix
t[m+1][n+1]
in that first row is full intialization and first column is full intialization 
then start from 1 to m and 1 to n (changing values) each box gives the subproblem and last box will be correct problem for that if we solve then we got the desried answer

see in starting of as n=0 and w=0 that's why we see answer 0 return 0