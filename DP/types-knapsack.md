Knapsach mainly have 3 types

Fractional Knapsack - (Greedy) - Less DP  

Example like storing value less than 1 or greater zero means we can divide the element in an array to find the optimal solution

0-1 Knapsack - (DP) - More DP

Example like storing the value as a whole take that element or not take that element we can keep the bag as empty like that

so 0-1 not 0.5

Unbounded Knapsack - (DP) 
here we can store the value like n number of times
