//dependency inversion principle
///high level module should not depend on low level module but rather they should depend on abstractions
//talk through contract(interfaces)
//high level module - where business logic is implemented
//low level module - where data is stored  
///need not to directly  talk through contract(interfaces)
//add one reference contract between high and low level module

//dependency injection