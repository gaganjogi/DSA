to run js code global execution context will be created

memory and code component

memory creation phase
when a function is called new execution context is created

see the code is exeuted twice(memory creation phase and code execution phase)

see the variables will be mapped as key in memory component and assigned undefined and for function exact code will be push 
and in code component phase if variable if assigned they will be updated in memory component and for function again new execution context is created in global code component after function is executed they will be popped from execution context

above all managed by callstack (bottom GEC and then FEC) and next FEC will be pushed