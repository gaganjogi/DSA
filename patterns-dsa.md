Patterns to check like in how much time complexity it can be completed

first
n<=20 then it will be mainly recursion back tracking brute force
1<n<10^5 - then it will be solved using two pointers greedy  O(n) or O(nlogn)

1<n<10^8> then it will be binary search O(1) or O(logn) math trick

if the question is like  trees DFS or BFS mostly traversal like pre order in order post order level order
if the question is graph then interconnected mostly DFS or BFS or union find

if the inpi